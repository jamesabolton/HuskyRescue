CREATE TABLE [dbo].[ApplicantResidenceOwnershipType] (
    [Id]    INT           NOT NULL,
    [Value] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_ApplicantResIdenceOwnershipType] PRIMARY KEY CLUSTERED ([Id] ASC)
);

